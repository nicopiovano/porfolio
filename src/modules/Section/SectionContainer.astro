---
const { class: className, id, sectionKey } = Astro.props
const computedKey = sectionKey ?? id
---

<section
  id={id}
  data-section={computedKey}
  data-section-key={sectionKey}
  class={`section ${className} scroll-mt-24 w-full mx-auto container lg:max-w-4xl md:max-w-2xl`}
  style="scroll-margin-top: 5rem;"
>
  <slot />
</section>
